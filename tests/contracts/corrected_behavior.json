{
  "schema_version": 1,
  "baseline": "stage0_behavior.json",
  "standalone_golden_atlases": {
    "BINARY_TREE": {
      "dimensions": [23, 23],
      "sha256": "7743d3330b127b497972c3ca2994e014f748d16cd03d59adbee428d9a60689b7",
      "pixel_sha256": "bdbf50896f80c900c9d09ddebe62d4c7a685437557fbb26a695506e155a7ec32",
      "correction": "Fractional placement bounds are rounded up instead of truncated.",
      "file_sha256_by_platform": {
        "windows-x64": "7743d3330b127b497972c3ca2994e014f748d16cd03d59adbee428d9a60689b7",
        "linux-x64": "764b4ca20d556c8ed27df246cd976df43039ac8380cfacdf2ec1f536a2526643",
        "macos-arm64": "7743d3330b127b497972c3ca2994e014f748d16cd03d59adbee428d9a60689b7"
      }
    }
  },
  "atlas_golden_comparison": {
    "asserted_value": "pixel_sha256",
    "rationale": "Decoded pixel content is identical on every supported platform. PNG file bytes are not: Pillow links zlib-ng on Windows and stock zlib on Linux, so the same image compresses differently. The retained sha256 and file_sha256_by_platform values are evidence of the encodings observed, not assertions.",
    "evidence": {
      "dimensions_match_across_platforms": true,
      "decoded_pixels_match_across_platforms": true,
      "windows_zlib": "1.3.1.zlib-ng",
      "linux_zlib": "1.3.1",
      "macos_note": "macOS arm64 produces byte-identical PNGs to Windows, so its file hash is the Windows value rather than a third one. That is consistent with the Windows and macOS Pillow wheels bundling zlib-ng while the manylinux wheel bundles stock zlib, and it is why the asserted value is the pixel hash: adding a platform required no new golden.",
      "macos_verified_on": "Apple M2, macOS 26.6.1, Blender 5.2.0 LTS"
    }
  },
  "dependency_path_trust": {
    "extended_length_paths_are_trusted": {
      "defect": "A healthy install reported dependency_conflict when the extension path was long enough that Blender reported PIL._imaging.__file__ in Windows extended-length form (\\\\?\\C:\\...), because the trust check compared it against a plain-form extension root.",
      "correction": "dependencies._inside strips extended-length syntax from both operands before comparing.",
      "regression_test": "tests/unit/test_dependencies_paths.py"
    },
    "lock_file_readable_beyond_max_path": {
      "defect": "A healthy install reported wheel_missing when the extension path exceeded MAX_PATH, because _load_lock read dependencies.lock.json without extended-length syntax. Blender's embedded Python is not long-path aware, so the read failed and the lock appeared absent.",
      "correction": "_load_lock reads the lock through _filesystem_path, as _wheel_integrity already did.",
      "regression_evidence": "tests/blender/verify_atlas_workflows.py run with a work directory whose extension path exceeds 260 characters; not reproducible under a standalone long-path-aware python.exe."
    }
  }
}
